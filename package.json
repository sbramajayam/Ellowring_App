{
  "name": "ellowring",
  "version": "1.0.0",
  "private": true,
  "description": "Ellowring — Learn. Prepare. Build. Get Hired.",
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm run start:dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "db:generate": "npm run prisma:generate --prefix backend",
    "db:push": "npm run prisma:push --prefix backend",
    "db:seed": "npm run prisma:seed --prefix backend",
    "setup": "npm install --prefix backend && npm install --prefix frontend && npm run db:generate && npm run db:push && npm run db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
